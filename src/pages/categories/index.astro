---
import Base from '../../layouts/Base.astro';
import { getPosts, groupByCategory } from '../../lib/content';

const groups = groupByCategory(await getPosts());
---

<Base title="Categories" description="Blog posts grouped by category." permalink="/categories/">
  <div class="reading">
    <p class="kicker">Index</p>
    <h1>Categories</h1>
  </div>
  {groups.map((group) => (
    <section class="taxonomy" id={group.slug}>
      <h2>{group.name} <span class="meta">({group.items.length})</span></h2>
      <ul>
        {group.items.map((item) => (
          <li>
            <a href={item.permalink}>{item.title}</a>
          </li>
        ))}
      </ul>
    </section>
  ))}
</Base>
