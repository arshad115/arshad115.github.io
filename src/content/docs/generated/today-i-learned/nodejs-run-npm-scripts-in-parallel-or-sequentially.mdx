---
title: "Running NPM Scripts in Parallel or Sequentially"
description: "Running NPM Scripts in Parallel or Sequentially - Today I Learned"
slug: "today-i-learned/nodejs/run-npm-scripts-in-parallel-or-sequentially"
contentType: "til"
category: "nodejs"
tags:
  - "nodejs"
sidebar:
  hidden: true
graph:
  visible: true
links:
  - "today-i-learned/nodejs/get-directory-name-and-filename-of-current-module"
  - "development/langur-language-translation-bot"
  - "development/my-first-open-source-repository-langur-translation-chat-bot"
  - "today-i-learned/android/circular-imageview-with-picasso"
  - "today-i-learned/android/create-region-android-studio"
  - "today-i-learned/android/load-data-in-webview-second-time"
  - "today-i-learned/android/show-logs-from-timinglogger"
  - "today-i-learned/android/string-array-in-xml"
---

# Running NPM Scripts in Parallel or Sequentially

When working with Node.js projects, you might need to run multiple NPM scripts either in parallel or sequentially. Here’s how you can achieve both.

## Running Scripts Sequentially

To run scripts one after another, you can use the `&&` operator. For example:

```json
{
    "scripts": {
        "script1": "echo 'Running script 1'",
        "script2": "echo 'Running script 2'",
        "start": "npm run script1 && npm run script2"
    }
}
```

In this example, `script2` will only run after `script1` has completed successfully.

## Running Scripts in Parallel
To run scripts simultaneously, you can use the `&` operator. For example:

```json
{
    "scripts": {
        "script1": "echo 'Running script 1'",
        "script2": "echo 'Running script 2'",
        "start": "npm run script1 & npm run script2"
    }
}
```

In this example, `script1` and `script2` will run at the same time.

## Conclusion

Using these methods, you can easily manage the execution order of your NPM scripts to fit your project's needs.
