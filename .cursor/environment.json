{
  "name": "Arshad Mehmood Blog (Jekyll)",
  "install": "bash .cursor/install.sh",
  "terminals": [
    {
      "name": "Jekyll server",
      "command": "export PATH=\"$HOME/.rbenv/bin:$PATH\"; eval \"$(rbenv init - bash)\"; bundle exec jekyll serve --host 0.0.0.0 --port 4000 --no-watch"
    }
  ],
  "ports": [
    {
      "name": "Jekyll server",
      "port": 4000
    }
  ]
}
